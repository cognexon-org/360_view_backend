{
  "name": "propertytour360-backend",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f api worker vision",
    "reset": "docker compose down -v"
  }
}
